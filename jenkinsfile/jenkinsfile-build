pipeline {
    agent any
    tools {
        nodejs 'node'
        jfrog 'jfrog-cli'
        }
    

    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
    }
}
